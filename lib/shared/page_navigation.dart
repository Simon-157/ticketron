import 'package:flutter/material.dart';
import 'bottom_wrapper.dart';

class PageNavigation extends StatelessWidget {
  const PageNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const BottomNavWrapper();
  }
}
